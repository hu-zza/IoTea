spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:mariadb://localhost:3306/IoTea_test?useUnicode=true
    username: IoTeaApp
    password: SK*2yWJ46N*WVxSY2%gaAySvYcRM!m$f
  flyway:
    connectRetries: 15
    connectRetriesInterval: 60

logging:
  level:
    hu.zza.iotea: DEBUG

server:
  error:
    include-message: always